apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-event-bus
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-event-bus
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-event-bus
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP