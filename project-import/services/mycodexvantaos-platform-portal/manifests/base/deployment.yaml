apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-app-portal
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-app-portal
    part-of: mycodexvantaos
    component: app-portal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-app-portal
  template:
    metadata:
      labels:
        app: mycodexvantaos-app-portal
        part-of: mycodexvantaos
        component: app-portal
    spec:
      containers:
        - name: mycodexvantaos-app-portal
          image: registry.local/mycodexvantaos/mycodexvantaos-app-portal:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-app-portal-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"