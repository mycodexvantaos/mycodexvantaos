apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-module-suite
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-module-suite
    part-of: mycodexvantaos
    component: module-suite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-module-suite
  template:
    metadata:
      labels:
        app: mycodexvantaos-module-suite
        part-of: mycodexvantaos
        component: module-suite
    spec:
      containers:
        - name: mycodexvantaos-module-suite
          image: registry.local/mycodexvantaos/mycodexvantaos-module-suite:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-module-suite-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"