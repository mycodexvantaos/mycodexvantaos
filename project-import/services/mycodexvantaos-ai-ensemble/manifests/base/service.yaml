apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-ai-engine
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-ai-engine
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-ai-engine
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP