apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-decision-engine
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-decision-engine
    part-of: mycodexvantaos
    component: decision-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-decision-engine
  template:
    metadata:
      labels:
        app: mycodexvantaos-decision-engine
        part-of: mycodexvantaos
        component: decision-engine
    spec:
      containers:
        - name: mycodexvantaos-decision-engine
          image: registry.local/mycodexvantaos/mycodexvantaos-decision-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-decision-engine-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"