apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-policy-engine
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-policy-engine
    part-of: mycodexvantaos
    component: policy-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-policy-engine
  template:
    metadata:
      labels:
        app: mycodexvantaos-policy-engine
        part-of: mycodexvantaos
        component: policy-engine
    spec:
      containers:
        - name: mycodexvantaos-policy-engine
          image: registry.local/mycodexvantaos/mycodexvantaos-policy-engine:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-policy-engine-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"