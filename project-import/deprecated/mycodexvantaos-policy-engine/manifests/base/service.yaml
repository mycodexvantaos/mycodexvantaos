apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-policy-engine
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-policy-engine
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-policy-engine
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP