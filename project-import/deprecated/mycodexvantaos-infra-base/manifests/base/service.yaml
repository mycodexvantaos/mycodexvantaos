apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-infra-base
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-infra-base
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-infra-base
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP