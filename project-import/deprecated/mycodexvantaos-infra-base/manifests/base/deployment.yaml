apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-infra-base
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-infra-base
    part-of: mycodexvantaos
    component: infra-base
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-infra-base
  template:
    metadata:
      labels:
        app: mycodexvantaos-infra-base
        part-of: mycodexvantaos
        component: infra-base
    spec:
      containers:
        - name: mycodexvantaos-infra-base
          image: registry.local/mycodexvantaos/mycodexvantaos-infra-base:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-infra-base-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"