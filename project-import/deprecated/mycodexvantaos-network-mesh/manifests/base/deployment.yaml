apiVersion: apps/v1
kind: Deployment
metadata:
  name: mycodexvantaos-network-mesh
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-network-mesh
    part-of: mycodexvantaos
    component: network-mesh
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycodexvantaos-network-mesh
  template:
    metadata:
      labels:
        app: mycodexvantaos-network-mesh
        part-of: mycodexvantaos
        component: network-mesh
    spec:
      containers:
        - name: mycodexvantaos-network-mesh
          image: registry.local/mycodexvantaos/mycodexvantaos-network-mesh:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mycodexvantaos-network-mesh-config
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"