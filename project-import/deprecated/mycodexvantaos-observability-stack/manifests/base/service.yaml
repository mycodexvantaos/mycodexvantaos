apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-observability-stack
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-observability-stack
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-observability-stack
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP