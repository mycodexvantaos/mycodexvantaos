apiVersion: v1
kind: Service
metadata:
  name: mycodexvantaos-control-cli
  namespace: mycodexvantaos
  labels:
    app: mycodexvantaos-control-cli
    part-of: mycodexvantaos
spec:
  selector:
    app: mycodexvantaos-control-cli
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP